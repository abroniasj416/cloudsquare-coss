# Frontend Notes

If you see garbled characters, run `npm run lint:encoding` and ensure files are UTF-8.
Vite does not auto-load `.env.example`. Copy `frontend/.env.example` to `frontend/.env`, then restart `npm run dev`.

## Quick Commands

```bash
npm install
cp .env.example .env
npm run lint:encoding
npm run dev
```
